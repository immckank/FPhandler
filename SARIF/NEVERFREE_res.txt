PUT/memcached/stats_prefix.c
{
  "classification": "FP",
  "reasoning": "The variable 'pfs' is assigned the return value of `stats_prefix_find`. If `stats_prefix_find` allocates new memory for a `PREFIX_STATS` object, it immediately adds this object to the global `prefix_stats` hash table before returning it. This means the allocated memory is transferred to a longer-lived global context and is intended to persist beyond the scope of `stats_prefix_record_set`. Therefore, the absence of a `free()` call for `pfs` within `stats_prefix_record_set` is expected behavior, aligning with the definition of a false positive for a NeverFree bug."
}
PUT/memcached/stats_prefix.c
{"classification": "FP", "reasoning": "The variable 'pfs' at memcached/stats_prefix.c:93 is assigned the return value of `stats_prefix_find`. The `stats_prefix_find` function either returns a pointer to an already existing `PREFIX_STATS` object from the global `prefix_stats` array, or it allocates a new `PREFIX_STATS` object and immediately stores it in the `prefix_stats` global array before returning the pointer. In both scenarios, the memory pointed to by 'pfs' is part of a longer-lived global data structure. Therefore, the memory is transferred to a longer-lived context and is not expected to be freed within `stats_prefix_record_get`."}
True
PUT/memcached/slab_automove_extstore.c
{"classification": "FP", "reasoning": "The memory allocated for 'a->window_data' at line 47 is part of the 'slab_automove' struct 'a'. The function `slab_automove_extstore_init` returns the pointer 'a' to its caller. This means ownership of 'a' and its members, including 'a->window_data', is transferred to the caller. Therefore, from the perspective of this function, the memory is not leaked; it has been transferred to a longer-lived context, making the alert a false positive."}
PUT/memcached/stats_prefix.c
{"classification": "FP", "reasoning": "The variable 'pfs' at line 107 receives a pointer from the `stats_prefix_find` function. If `stats_prefix_find` allocates new memory for `pfs` (using `calloc`), it immediately links this newly allocated structure into the global `prefix_stats` array. This means the ownership of the allocated memory is transferred to a longer-lived context (the global data structure). Therefore, the `stats_prefix_record_delete` function is not responsible for freeing this memory, and the alert is a false positive according to the provided guidance."}
True
PUT/memcached/slab_automove.c
{"classification": "FP", "reasoning": "The memory for 'a->window_data' is allocated, and then the parent structure 'a' (which contains 'a->window_data') is returned by the function. This indicates that the ownership of the allocated memory, including 'a->window_data', is transferred to the caller. The caller is responsible for freeing this memory, satisfying the condition that the memory has been transferred to a longer-lived context. Therefore, the memory is not leaked within this function."}
PUT/memcached/memcached.c
{
  "classification": "TP",
  "reasoning": "The variable `storage_cf` is allocated via `calloc` in `storage_init_config` and assigned to `storage_cf` at `memcached/memcached.c:4866`. There are several execution paths within `main` where this memory is not freed:\n\n1.  **Error path via `storage_read_config`**: Inside the `while` loop parsing options, if a suboption is processed and `storage_read_config(storage_cf, &subopts_temp)` returns a non-zero value, the code jumps to the `error:` label. At this label, `subopts_orig` is freed, but `storage_cf` is not freed before `main` returns `1`. This path constitutes a memory leak for `storage_cf`.\n\n2.  **Normal execution path**: If the program runs successfully and reaches the end of `main` (e.g., when `stop_main_loop` condition is met and `retval` is returned), `storage_cf` is never explicitly freed. While `storage_cf` is passed to `storage_init` which sets the global `ext_storage`, there is no corresponding `storage_free` or `free(ext_storage)` call at the end of `main` to clean up the allocated storage configuration (or the storage itself). This indicates that the memory pointed to by `storage_cf` (and potentially the entire `ext_storage` structure it initializes) is not released before program termination."
}
PUT/memcached/crawler.c
{"classification": "FP", "reasoning": "The memory allocated at `crawler.c:164` using `calloc` is assigned to `cm->data` within the `crawler_expired_init` function. This indicates that the ownership of the allocated memory is transferred to the `crawler_module_t` structure, which represents a longer-lived context. Therefore, the memory is not expected to be freed within this function, making the alert a False Positive according to the provided guidance."}
True
PUT/memcached/storage.c
{"classification": "FP", "reasoning": "The memory allocated for 'tmp' at `storage.c:1524` by `storage_conf_parse` is subsequently assigned to `cf->storage_file`. The `cf` variable is a pointer to a `storage_settings` structure that is passed into the `storage_read_config` function as `void *conf`. This indicates that the ownership of the allocated memory is transferred to a longer-lived context (the `conf` structure), meaning it is not the responsibility of the `storage_read_config` function to free this memory. This aligns with the guidance for classifying a false positive for a NeverFree bug."}